import React from "react";
import product from "./Product";

export default function Name({ name }) {
  return (
    <>
      <h2 className="font-bold text-red-800">{product.name}</h2>;
    </>
  );
}
